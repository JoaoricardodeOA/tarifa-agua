spring:
  application:
    name: tarifa-agua
  datasource:
    url: jdbc:postgresql://localhost:5432/tarifa_agua
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always
